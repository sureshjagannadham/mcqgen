# ===========================
# requirements.txt (Project Dependencies)
# ===========================

# Core libraries needed for your MCQ Generator project

openai          # Official OpenAI Python client (connects to GPT models)
langchain       # Framework for building LLM apps (chains, prompts, agents)
streamlit       # Web app framework (used to build the quiz UI)
python-dotenv   # Loads environment variables from .env file (like API keys)
pypdf           # Modern library to read PDF files (extracts text from uploaded PDFs)

-e .            # Editable install of your own project (the "src" folder)
                # Means: install this project as a package in development mode
                # So you can import your own modules like src.mcqgenerator.utils
# ===========================

# WHY THIS FILE EXISTS:
# ---------------------------
# - requirements.txt lists all external libraries your project depends on.
# - When someone installs your project, they can run:
#       pip install -r requirements.txt
#   → This ensures all necessary packages are installed automatically.
# - The "-e ." line makes your project itself installable as a package,
#   so you can import your own code cleanly without messy relative paths.
# ===========================
